# 2026-06-22 - 宠物商城全功能实现

## 完成工作
根据用户需求清单，实现了所有缺失的前端和后端功能。

### 后端新增（17个文件）
- **实体类**: Address, Store, Video, Review 四个新实体
- **修改实体**: User(添加nickname/avatar/memberLevel), Product(添加storeId/productType/videoId), Order(完整状态流转0~4/-1~-4)
- **DTO类**: UserProfileRequest, AddressRequest, StoreRequest, VideoRequest, ReviewRequest, OrderReturnRequest, AiChatRequest
- **Repository**: AddressRepository, StoreRepository, VideoRepository, ReviewRepository
- **Service**: UserService, AddressService, StoreService, VideoService, ReviewService, AiService
- **Controller**: UserController, AddressController, StoreController, VideoController, ReviewController, AiController
- **修改**: OrderService(完整状态机), AuthService(新字段), DataInitializer(商店/视频演示数据), InterceptorConfig(公开路径)

### 前端新增（12个新页面）
- 个人中心(Profile.vue)、收货地址(Addresses.vue)、商店浏览(Stores.vue)、商店详情(StoreDetail.vue)
- 视频浏览(Videos.vue)、视频详情(VideoDetail.vue)、AI助手(AiChat.vue)、订单详情(OrderDetail.vue)
- 管理后台: Stores.vue, Videos.vue, Users.vue, Reviews.vue
- **修改页面**: Layout.vue, Orders.vue, ProductDetail.vue, Checkout.vue, AdminLayout.vue, Dashboard.vue, admin/Orders.vue

### 技术栈
- 后端: Spring Boot 3.2.5 + SQLite + JPA + JWT
- 前端: Vue 3 + Element Plus + Vue Router + Pinia + Axios
- 前端构建成功，57个文件成功编译

### 智谱AI接入 (11:56)
- AiService 接入智谱AI GLM-4.7-Flash 模型 (model=glm-4-flash)
- API Key: 59447db44fac49e186c66e7f64cf8022.cBBQOxhjydIDHY16
- 使用 JDK 内置 HttpClient 调用，无需额外依赖
- 系统提示词注入商品数据库上下文，支持多轮对话历史
- API调用失败时自动降级为规则匹配fallback
- AiChatRequest 新增 history 字段支持多轮对话
- 前端 AiChat.vue 发送最近8轮对话历史
